"""Concrete ``Retriever`` implementations.

``LexicalRetriever`` (OpenSearch) is the only one today. Qdrant/Neo4j retrievers will join here, each
implementing the same ``src.search.contracts.Retriever`` protocol.
"""
